<Page x:Class="ProgramacionIII_Unidad1.AlgoritmosDeBusqueda"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="900" d:DesignWidth="1280"
      Title="AlgoritmosDeBusqueda">

    <Page.Resources>
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#5B21B6"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.82"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SearchButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#312E81"/>
            <Setter Property="Background" Value="#EEF2FF"/>
            <Setter Property="BorderBrush" Value="#D8DDF8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E5E7FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="35">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,22">
                <Button x:Name="BtnVolver"
                        Content="←"
                        Width="42"
                        Height="42"
                        Background="White"
                        Foreground="#6D28D9"
                        Style="{StaticResource RoundedButton}"
                        Click="BtnVolver_Click"/>

                <StackPanel Margin="18,0,0,0">
                    <TextBlock Text="Algoritmos de Búsqueda"
                               FontSize="36"
                               FontWeight="Bold"
                               Foreground="#7E22CE"/>
                    <TextBlock Text="Encuentra elementos usando diferentes estrategias de búsqueda"
                               FontSize="15"
                               Foreground="#64748B"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </StackPanel>

            <Border Grid.Row="1" Background="White" CornerRadius="18" Padding="28">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.12"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Configuración del Arreglo" FontSize="18" FontWeight="Bold" Foreground="#1F2937"/>
                    <TextBlock Text="Agrega tus propios elementos al vector" FontSize="14" Foreground="#64748B" Margin="0,5,0,18"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Text="Agregar elemento" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,6"/>
                        <TextBox x:Name="TxtElemento" Grid.Row="1" Grid.Column="0" Style="{StaticResource RoundedTextBox}" Margin="0,0,18,0"/>
                        <Button x:Name="BtnAgregar" Grid.Row="1" Grid.Column="1" Content="＋ Agregar" Background="#A78BFA" Style="{StaticResource RoundedButton}" Click="BtnAgregar_Click"/>
                    </Grid>

                    <TextBlock Text="Vector" FontWeight="Bold" Foreground="#1E293B" Margin="0,28,0,10"/>
                    <Border Background="#EEF2FF" BorderBrush="#CDD6FE" BorderThickness="1" CornerRadius="10" Padding="12" MinHeight="78">
                        <ItemsControl x:Name="ICVector">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Click="BtnEliminarElemento_Click"
                                            Tag="{Binding}"
                                            Background="White"
                                            BorderBrush="#D8B4FE"
                                            BorderThickness="2"
                                            Padding="14,10"
                                            Margin="4"
                                            Cursor="Hand">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="Border"
                                                        Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="8"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Border" Property="Background" Value="#F5F3FF"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Valor}" Foreground="#9333EA" FontWeight="Bold" FontSize="16"/>
                                        </StackPanel>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>

                    <TextBlock Text="Valor a buscar" FontWeight="Bold" Foreground="#1E293B" Margin="0,22,0,8"/>
                    <TextBox x:Name="TxtBuscar" Style="{StaticResource RoundedTextBox}"/>

                    <TextBlock Text="Elegir algoritmo de búsqueda" FontWeight="Bold" Foreground="#1E293B" Margin="0,22,0,10"/>
                    <UniformGrid Columns="3" Rows="2">
                        <Button x:Name="BtnLineal" Content="Búsqueda Lineal" Style="{StaticResource SearchButtonStyle}" Click="BtnLineal_Click"/>
                        <Button x:Name="BtnCentinela" Content="Lineal con Centinela" Style="{StaticResource SearchButtonStyle}" Click="BtnCentinela_Click"/>
                        <Button x:Name="BtnBinaria" Content="Búsqueda Binaria" Style="{StaticResource SearchButtonStyle}" Click="BtnBinaria_Click"/>
                        <Button x:Name="BtnIndexada" Content="Búsqueda Indexada" Style="{StaticResource SearchButtonStyle}" Click="BtnIndexada_Click"/>
                        <Button x:Name="BtnHashing" Content="Hashing" Style="{StaticResource SearchButtonStyle}" Click="BtnHashing_Click"/>
                        <Border Background="Transparent"/>
                    </UniformGrid>

                    <Border Background="#FFF7FB" BorderBrush="#F3D3E8" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,16,0,0">
                        <StackPanel>
                            <WrapPanel Margin="0,0,0,8">
                                <TextBlock Text="Complejidad:" FontWeight="Bold" Foreground="#374151" VerticalAlignment="Center"/>
                                <Border Background="#C7D2FE" CornerRadius="8" Margin="10,0,0,0" Padding="10,4">
                                    <TextBlock x:Name="TxtComplejidad" Text="-" Foreground="#4338CA" FontWeight="Bold"/>
                                </Border>
                            </WrapPanel>
                            <WrapPanel>
                                <TextBlock Text="Resultado:" FontWeight="Bold" Foreground="#374151" VerticalAlignment="Center"/>
                                <Border x:Name="ResultadoBadge" Background="#FCA5A5" CornerRadius="8" Margin="10,0,0,0" Padding="10,4">
                                    <TextBlock x:Name="TxtResultado" Text="Aún no ejecutado" Foreground="White" FontWeight="Bold"/>
                                </Border>
                            </WrapPanel>
                            <TextBlock x:Name="TxtDetalle" Margin="0,12,0,0" Foreground="#64748B" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
