<Page x:Class="ProgramacionIII_Unidad1.EstructurasNoLineales.Grafos"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="900" d:DesignWidth="1280"
      Title="Grafos"
      Background="#F6F7FB">

    <Page.Resources>
        <SolidColorBrush x:Key="BgPage" Color="#F6F7FB"/>
        <SolidColorBrush x:Key="BgCard" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Primary" Color="#5B5BD6"/>
        <SolidColorBrush x:Key="PrimaryHover" Color="#4848B8"/>
        <SolidColorBrush x:Key="PrimarySoft" Color="#ECECFF"/>
        <SolidColorBrush x:Key="Danger" Color="#D94F70"/>
        <SolidColorBrush x:Key="BorderSoft" Color="#E3E7F0"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#1F2430"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#667085"/>
        <SolidColorBrush x:Key="CanvasBg" Color="#FCFCFE"/>

        <DropShadowEffect x:Key="CardShadow" Color="#15000000" BlurRadius="18" ShadowDepth="2" Opacity="0.18"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource BgCard}"/>
            <Setter Property="CornerRadius" Value="18"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
        </Style>

        <Style x:Key="TitleText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI Semibold"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock" BasedOn="{StaticResource TitleText}">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
        </Style>

        <Style x:Key="CaptionText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FAFBFF"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}"/>
            <Setter Property="BorderThickness" Value="1.2"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,9"/>
            <Setter Property="MinHeight" Value="42"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="CheckStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="MinHeight" Value="42"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI Semibold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource PrimaryHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.88"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="{StaticResource PrimarySoft}"/>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource Danger}"/>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource BgPage}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="{StaticResource BorderSoft}" BorderThickness="0,0,0,1" Padding="28,20">
            <StackPanel>
                <TextBlock Text="Grafos" Style="{StaticResource TitleText}" FontSize="28"/>
                <TextBlock Text="Gestiona vertices y aristas, visualiza el grafo, consulta matriz y ejecuta recorridos BFS y DFS con algoritmos propios." Style="{StaticResource CaptionText}" Margin="0,6,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="Operaciones" Style="{StaticResource SectionTitle}"/>

                            <TextBlock Text="Vertice" Style="{StaticResource LabelText}"/>
                            <TextBox x:Name="txtVertice" Style="{StaticResource InputStyle}"/>
                            <Button Content="Agregar vertice" Click="BtnAgregarVertice_Click" Style="{StaticResource PrimaryButton}" Margin="0,12,0,10"/>
                            <Button Content="Eliminar vertice" Click="BtnEliminarVertice_Click" Style="{StaticResource DangerButton}"/>

                            <Separator Margin="0,18,0,18"/>

                            <TextBlock Text="Origen" Style="{StaticResource LabelText}"/>
                            <TextBox x:Name="txtOrigen" Style="{StaticResource InputStyle}"/>
                            <TextBlock Text="Destino" Style="{StaticResource LabelText}" Margin="0,12,0,6"/>
                            <TextBox x:Name="txtDestino" Style="{StaticResource InputStyle}"/>
                            <CheckBox x:Name="chkDirigido" Content="Arista dirigida" Style="{StaticResource CheckStyle}"/>

                            <WrapPanel Margin="0,14,0,0">
                                <Button Content="Agregar arista" Click="BtnAgregarArista_Click" Style="{StaticResource PrimaryButton}" Margin="0,0,10,10"/>
                                <Button Content="Eliminar arista" Click="BtnEliminarArista_Click" Style="{StaticResource SecondaryButton}" Margin="0,0,0,10"/>
                            </WrapPanel>

                            <Separator Margin="0,12,0,18"/>

                            <TextBlock Text="Inicio de recorrido" Style="{StaticResource LabelText}"/>
                            <TextBox x:Name="txtInicioRecorrido" Style="{StaticResource InputStyle}"/>
                            <WrapPanel Margin="0,14,0,0">
                                <Button Content="BFS" Click="BtnBfs_Click" Style="{StaticResource PrimaryButton}" Margin="0,0,10,10"/>
                                <Button Content="DFS" Click="BtnDfs_Click" Style="{StaticResource SecondaryButton}" Margin="0,0,10,10"/>
                                <Button Content="Limpiar" Click="BtnLimpiar_Click" Style="{StaticResource DangerButton}" Margin="0,0,0,10"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Margin="0,16,0,0">
                        <StackPanel>
                            <TextBlock Text="Resumen" Style="{StaticResource SectionTitle}"/>
                            <TextBlock x:Name="txtEstado" Text="Listo para operar" Style="{StaticResource CaptionText}" TextWrapping="Wrap"/>
                            <TextBlock Text="Lista de adyacencia" Style="{StaticResource LabelText}" Margin="0,14,0,6"/>
                            <TextBox x:Name="txtListaAdyacencia" TextWrapping="Wrap" AcceptsReturn="True" IsReadOnly="True" Height="180" VerticalScrollBarVisibility="Auto" Style="{StaticResource InputStyle}"/>
                            <TextBlock Text="Matriz de adyacencia" Style="{StaticResource LabelText}" Margin="0,14,0,6"/>
                            <TextBox x:Name="txtMatriz" TextWrapping="NoWrap" AcceptsReturn="True" IsReadOnly="True" Height="180" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Style="{StaticResource InputStyle}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CardStyle}" Padding="16,14">
                        <StackPanel>
                            <TextBlock Text="Vertices" Style="{StaticResource CaptionText}"/>
                            <TextBlock x:Name="txtTotalVertices" Text="0" FontSize="32" FontWeight="Bold" Foreground="{StaticResource Primary}"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource CardStyle}" Padding="16,14" Margin="12,0,0,0">
                        <StackPanel>
                            <TextBlock Text="Aristas" Style="{StaticResource CaptionText}"/>
                            <TextBlock x:Name="txtTotalAristas" Text="0" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource CardStyle}" Padding="16,14" Margin="12,0,0,0">
                        <StackPanel>
                            <TextBlock Text="Recorrido" Style="{StaticResource CaptionText}"/>
                            <TextBlock x:Name="txtRecorridoResultado" Text="Sin ejecutar" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Margin="18,18,18,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Visualizador del grafo" Style="{StaticResource SectionTitle}" Margin="0"/>
                                <TextBlock Text="Los vertices se distribuyen automaticamente en circulo para una vista clara." Style="{StaticResource CaptionText}" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Grid>

                        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                            <Canvas x:Name="canvasGrafo" Width="900" Height="600" Background="{StaticResource CanvasBg}"/>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Page>
